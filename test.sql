DROP TABLE IF EXISTS USER;
DROP TABLE IF EXISTS BOOK;

CREATE TABLE USER(
  ID INT,
  NAME VARCHAR(200)
);

CREATE TABLE BOOK(
  ID INT,
  NAME VARCHAR(200)
);

#查看Mysql的默认事务隔离机制
SELECT @@TX_ISOLATION;